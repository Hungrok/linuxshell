# install prerequisite packages
apt install libopenblas-dev libblas-dev m4 cmake cython git

# source download 
mkdir torchinst 
cd torchinst
git clone --recursive https://github.com/pytorch/pytorch
cd pytorch

# Setup env. Varaibles 
export NO_CUDA=1
export NO_DISTRIBUTED=1
export NO_MKLDNN=1
export NO_NNPACK=1
export NO_QNNPACK=1

# Build
python3 setup.py build

# Verify 
Python3
Import torch 








  
